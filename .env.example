# Example Wi-Fi credentials (safe to commit)
WIFI_SSID=YourNetworkSSID
WIFI_PASSWORD=YourStrongPassword

# Distance threshold in millimeters (e.g., 200 = 20 cm)
DIST_THRESHOLD_MM=200

# Temperature threshold in Celsius to change display image
TEMP_THRESHOLD_C=30.0
